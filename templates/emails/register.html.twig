<h1>Activez votre compte</h1>
<p>Bonjour {{ user.pseudo }},</p>
<p>Votre inscription sur le site Burger Tech est à valider en cliquant sur le lien ci-dessous :</p>
<p><a href="{{ absolute_url(path('verify_user', {token: token})) }}">Lien</a></p> 
<p>Ce lien expirera dans 10 minutes. Tic Tac Tic Tac...</p>
 


{# <p><a href="{{ absolute_url(path('verify_user', {token: token})) }}">{{ absolute_url(path('verify_user', {token: token})) }}</a></p> #}